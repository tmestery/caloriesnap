name: CALORIE_QUEST
description: A Flutter app for scanning and splitting bills.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.0
  tflite: ^1.1.2
  http: ^1.0.0
  shared_preferences: ^2.0.15
  fl_chart: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/mobilenet_v1_0.25_128.tflite
    - assets/labels.txt